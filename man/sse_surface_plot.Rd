% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sse_surface_plot.R
\name{sse_surface_plot}
\alias{sse_surface_plot}
\title{Plot the SSE landscape around a set of LPPLS parameter values}
\usage{
sse_surface_plot(
  log_price,
  t = seq_along(log_price),
  params,
  vary,
  span = NULL,
  mode = 0,
  static = FALSE,
  ...
)
}
\arguments{
\item{log_price}{Numeric vector of observed log-prices.}

\item{t}{Numeric vector of time indices matching \code{log_price}
(default \code{seq_along(log_price)}).}

\item{params}{Named list of LPPLS parameter values, e.g.
\code{list(A =, B =, C1 =, C2 =, tc =, m =, omega =)}, plus any mode-specific
parameters. Supplies the fixed values for the parameters that are not swept,
and the centre value for those that are.}

\item{vary}{Character vector of length 2 naming the two parameters to sweep
(for example \code{c("m", "omega")} or \code{c("tc", "C2")}).}

\item{span}{Optional named list giving the sweep range per varied parameter.
Each entry is either a length-2 numeric \code{c(lower, upper)} (an explicit range)
or a single numeric half-width \code{h} (range \code{centre} +/- \code{h}). Parameters not
listed fall back to a per-parameter default: \code{tc}, centre +/- 90; \code{m},
\code{0.1}-\code{0.9}; \code{omega}, \code{1}-\code{20}; \code{C2} as a phase, \code{1}-\code{20}; other (linear)
parameters, centre +/- |centre|.}

\item{mode}{Integer LPPLS mode (\code{0}-\code{3}); default \code{0}.}

\item{static}{Logical; \code{FALSE} (default) returns an interactive \code{plotly}
surface, \code{TRUE} draws a static base-R \code{persp()} surface to the current device.}

\item{...}{When \code{static = TRUE}, extra arguments passed to \code{\link[graphics:persp]{graphics::persp()}}
(e.g. \code{theta}, \code{phi}).}
}
\value{
For \code{static = FALSE}, a \code{plotly} surface object. For \code{static = TRUE},
the SSE grid (\code{list(x, y, z)}) returned invisibly after drawing the surface.
}
\description{
Convenience wrapper around \code{\link[=contour_plot_sse]{contour_plot_sse()}} that visualises the sum of
squared errors (SSE) of the LPPLS model over a grid of two chosen parameters,
holding the rest fixed at supplied values. Axis labels follow each parameter's
meaning under the given \code{mode} (in the higher-order Landau modes the \code{C2} slot
is the log-periodic phase and is labelled \code{phi}). By default an interactive
3-D \code{plotly} surface is returned (no colour bar: the colours convey depth, and
the z-axis already is SSE); set \code{static = TRUE} for a base-R \code{persp()} render
suitable for print/PDF.
}
\examples{
\donttest{
set.seed(3630)
n  <- 1000
lp <- vapply(seq_len(n), function(i)
  eval_lppls(i, A = 8, B = -0.025, C1 = 0.0015, C2 = 0.001,
             tc = n + 1, m = 0.6, omega = 8) + stats::rnorm(1) * 0.05,
  numeric(1))

## SSE over (m, omega), everything else fixed -- interactive surface.
sse_surface_plot(
  log_price = lp[1:910], t = 1:910,
  params = list(tc = 1001, m = 0.6, omega = 8, A = 8, B = -0.025,
                C1 = 0.0015, C2 = 0.001),
  vary = c("m", "omega"))

## SSE over (tc, phi) in the 1st-order Landau mode, explicit ranges, static.
sse_surface_plot(
  log_price = lp[1:910], t = 1:910,
  params = list(tc = 1001, m = 0.6, omega = 8, A = 8, B = -0.025, C1 = 0.0015),
  vary = c("tc", "C2"), span = list(tc = c(911, 1090), C2 = c(1, 20)),
  mode = 1, static = TRUE)
}
}
\seealso{
\code{\link[=contour_plot_sse]{contour_plot_sse()}}, \code{\link[=SSE]{SSE()}}, \code{\link[=eval_lppls]{eval_lppls()}}
}
