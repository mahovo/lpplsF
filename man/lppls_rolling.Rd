% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lppls_rolling.R
\name{lppls_rolling}
\alias{lppls_rolling}
\title{Rolling-Window LPPLS Calibration (rolling \eqn{T_1}, fixed \eqn{T_2})}
\usage{
lppls_rolling(
  log_price,
  t1_seq,
  hold_out = 0,
  fh = 30,
  tc_init = NULL,
  fit_args = list(),
  fb = FALSE
)
}
\arguments{
\item{log_price}{Numeric vector of log-prices for the full series.}

\item{t1_seq}{Integer vector of rolling start indices (absolute positions into
\code{log_price}). All entries must be \verb{< T2 = length(log_price) - hold_out}.}

\item{hold_out}{Integer, number of observations reserved from the end of the
full series for the forecast/validation region (default 0). Fixes \eqn{T_2}.}

\item{fh}{Integer, forecast horizon passed to \code{\link[=fit_lppls]{fit_lppls()}} (default 30).}

\item{tc_init}{Numeric or \code{NULL}. Initial critical time for \code{\link[=fit_lppls]{fit_lppls()}}. If
\code{NULL} (default) a per-window value \code{n_cal + floor(fh / 2)} is used, where
\code{n_cal} is the window's calibration length.}

\item{fit_args}{Named list of additional arguments forwarded to \code{\link[=fit_lppls]{fit_lppls()}}
(e.g. \code{lower}, \code{upper}, \code{m_init}, \code{o_init}, \code{num_searches}, \code{mode},
\code{mpl_cutoff}, \code{factr}). The arguments \code{log_price}, \code{fh}, \code{hold_out},
\code{tc_init} and \code{fb} are controlled by \code{lppls_rolling()} and are ignored if
supplied here. If \code{mode} is not supplied it defaults to \code{"MPL"}.}

\item{fb}{Logical, whether to print per-window feedback (default \code{FALSE}).}
}
\value{
An object of class \code{"lppls_rolling"}: a list with
\describe{
\item{table}{Data frame with one row per window: \code{T1}, \code{n_cal}, the
absolute point estimates \code{tc}, \code{m}, \code{omega}, \code{A}, \code{B}, \code{C1}, \code{C2}, \code{D},
the calibration-region residual sum of squares \code{sse_cal}, the absolute
likelihood-interval bounds \code{LI5l}/\code{LI5u}/\code{LI10l}/\code{LI10u}/\code{LI50l}/\code{LI50u}
and the absolute modified-profile-likelihood estimate \code{tc_hat_mpl}.}
\item{fits}{List of best-fit parameter lists (\code{fit[[1]]}) per window, in
window-local time.}
\item{t1_seq, T2, hold_out, fh, n_full}{The calibration setup.}
\item{fit_args}{The effective list of arguments forwarded to \code{\link[=fit_lppls]{fit_lppls()}}
(after stripping the controlled ones and defaulting \code{mode}); recorded so
\code{\link[=lr_trend_test]{lr_trend_test()}} can re-roll bootstrap replicates with the same settings.}
}
}
\description{
Calibrates a sequence of LPPLS models that share a common calibration end
\eqn{T_2} while the start time \eqn{T_1} rolls over \code{t1_seq}. This is the
multi-window primitive underlying both the window-diagnostic plots
(\code{\link[=rolling_tc_plot]{rolling_tc_plot()}}, \code{\link[=rolling_param_plot]{rolling_param_plot()}}) and Lagrange regularization
(\code{\link[=lppls_lagrange]{lppls_lagrange()}}): the (expensive) calibrations are computed once here and
reused by all downstream analyses.
}
\details{
For each \eqn{T_1} in \code{t1_seq} the model is calibrated on
\code{log_price[T1:length(log_price)]} via \code{\link[=fit_lppls]{fit_lppls()}}. The last \code{hold_out}
observations of every window are reserved for the forecast/validation region,
so the calibration region is the absolute interval \eqn{[T_1, T_2]} with the
common end \eqn{T_2 = \texttt{length(log\_price)} - \texttt{hold\_out}} held
fixed across windows.

Because \code{fit_lppls()} re-indexes each window to start at 1, all per-window
results (\code{tc}, the likelihood intervals and \code{tc_hat_mpl}) are shifted back to
absolute time by adding \eqn{T_1 - 1} before being returned.

The likelihood-interval columns (\code{LI5l} ... \code{tc_hat_mpl}) are populated only
when the fit is run in \code{mode = "MPL"} (the default); under \code{mode = "F2"} they
are \code{NA} but the point estimates and \code{sse_cal} (hence Lagrange) are identical.
}
\examples{
\dontrun{
set.seed(3630)
N <- 2000
log_p <- eval_lppls(seq_len(N), A = 8, B = -0.025, C1 = 0.0015, C2 = 0.001,
                    tc = N + 1, m = 0.6, omega = 8) + rnorm(N) * 0.05
roll <- lppls_rolling(
  log_price = log_p,
  t1_seq    = seq(from = 1, by = 10, length.out = 101),
  hold_out  = 100, fh = 150,
  fit_args  = list(lower = c(0.1, 6, -0.03, 0.8),
                   upper = c(0.9, 13, -1e-14, 1e6),
                   m_init = 0.2, o_init = 12, num_searches = 1)
)
print(rolling_tc_plot(roll))
}

}
\seealso{
\code{\link[=fit_lppls]{fit_lppls()}}, \code{\link[=lppls_lagrange]{lppls_lagrange()}}, \code{\link[=rolling_tc_plot]{rolling_tc_plot()}},
\code{\link[=rolling_param_plot]{rolling_param_plot()}}
}
