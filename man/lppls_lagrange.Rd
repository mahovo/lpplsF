% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lppls_lagrange.R
\name{lppls_lagrange}
\alias{lppls_lagrange}
\title{Lagrange Regularization of the LPPLS Objective Function}
\usage{
lppls_lagrange(
  log_price,
  t1_seq,
  hold_out = 0,
  fh = 30,
  p = 7,
  tc_init = NULL,
  fit_args = list(),
  fb = FALSE
)
}
\arguments{
\item{log_price}{Numeric vector of log-prices for the full series. Windows
are taken as \code{log_price[T1:length(log_price)]} for each \code{T1} in \code{t1_seq}.}

\item{t1_seq}{Integer vector of rolling start indices (absolute positions
into \code{log_price}). All entries must be \verb{< T2 = length(log_price) - hold_out}.}

\item{hold_out}{Integer, number of observations reserved from the end of the
full series for the forecast/validation region (default 0). Fixes the
common calibration end \eqn{T_2}.}

\item{fh}{Integer, forecast horizon passed to \code{\link[=fit_lppls]{fit_lppls()}} (default 30).}

\item{p}{Integer, number of parameters subtracted in the \eqn{\chi^2_{np}}
denominator (default 7). Use \code{p = 0} to omit the correction (thesis form).}

\item{tc_init}{Numeric or \code{NULL}. Initial critical time for \code{\link[=fit_lppls]{fit_lppls()}}.
If \code{NULL} (default) a per-window value \code{n_cal + floor(fh / 2)} is used,
matching the thesis, where \code{n_cal} is the window's calibration length.}

\item{fit_args}{Named list of additional arguments forwarded to
\code{\link[=fit_lppls]{fit_lppls()}} (e.g. \code{lower}, \code{upper}, \code{m_init}, \code{o_init}, \code{num_searches},
\code{mode}, \code{mpl_cutoff}, \code{factr}). The arguments \code{log_price}, \code{fh},
\code{hold_out}, \code{tc_init} and \code{fb} are controlled by \code{lppls_lagrange()} and are
ignored if supplied here. If \code{mode} is not supplied it defaults to \code{"F2"},
which yields the same point estimate as \code{"MPL"} for the Lagrange
computation.}

\item{fb}{Logical, whether to print per-window feedback (default \code{FALSE}).}
}
\value{
An object of class \code{"lppls_lagrange"}: a list with
\describe{
\item{table}{Data frame with columns \code{T1}, \code{n} (calibration length),
\code{chi_sq_np} and \code{chi_sq_lambda}.}
\item{fits}{List of best-fit parameter lists (\code{fit[[1]]}) per window.}
\item{trend}{The fitted \code{lm} object for \code{chi_sq_np ~ T1}.}
\item{intercept, slope}{OLS trend coefficients \eqn{\beta_0, \beta_1}.}
\item{lambda}{The regularization multiplier \eqn{\lambda^* = -\beta_1}.}
\item{T2}{The common calibration end \eqn{T_2}.}
\item{p}{The parameter count used in the denominator.}
\item{t1_star}{The optimal start time \eqn{T_1^*}.}
}
}
\description{
Determines the optimal calibration start time \eqn{T_1^*} by Lagrange
regularization of the normalized residual sum of squares, following
Demos & Sornette (2019). A sequence of LPPLS models is calibrated with a
fixed end time \eqn{T_2} and a rolling start time \eqn{T_1}. The normalized
residual sum of squares \eqn{\chi^2_{np}(T_1)} is corrected for the linear
bias induced by the shrinking number of observations, and \eqn{T_1^*} is the
minimizer of the regularized objective \eqn{\chi^2_{\lambda^*}(T_1)}.
}
\details{
For each start time \eqn{T_1} in \code{t1_seq}, the model is calibrated on the
window \code{log_price[T1:length(log_price)]}. The last \code{hold_out} observations of
every window are reserved for the forecast/validation region, so the
calibration region is \eqn{[T_1, T_2]} with the common end
\eqn{T_2 = \texttt{length(log\_price)} - \texttt{hold\_out}}, held fixed
across windows.

The normalized residual sum of squares is
\deqn{\chi^2_{np}(T_1) = \frac{1}{(T_2 - T_1) - p} \sum_{i = T_1}^{T_2} r_i^2,}
where \eqn{r_i} is the residual of calibration observation \eqn{i} and \eqn{p}
is the number of model parameters. Setting \code{p = 0} reproduces the
\dQuote{p-correction omitted} form used in the thesis; \code{p = 7} (the default)
subtracts the full LPPLS parameter count
\eqn{\Psi = (t_c, m, \omega, A, B, C_1, C_2)}.

The bias is modelled as a linear trend
\eqn{\chi^2_{np}(T_1) = \beta_0 + \beta_1 T_1} fitted by ordinary least
squares. The regularized objective is
\deqn{\chi^2_{\lambda^*}(T_1) = \chi^2_{np}(T_1) - \lambda^* (T_2 - T_1),
  \qquad \lambda^* = -\beta_1,}
which has zero trend in \eqn{T_1}. The optimal start time is
\eqn{T_1^* = \arg\min_{T_1} \chi^2_{\lambda^*}(T_1)}.

Adding or removing a constant from the regularized objective does not change
its minimizer, so the choice of \eqn{T_2} in the \eqn{-\lambda^*(T_2 - T_1)}
term shifts the curve vertically but leaves \eqn{T_1^*} unchanged.
}
\examples{
\dontrun{
set.seed(3630)
N <- 2000
tc <- N + 1
t <- seq_len(N)
log_p <- eval_lppls(t, A = 8, B = -0.025, C1 = 0.0015, C2 = 0.001,
                    tc = tc, m = 0.6, omega = 8) + rnorm(N) * 0.05

lag <- lppls_lagrange(
  log_price = log_p,
  t1_seq    = seq(from = 1, by = 10, length.out = 101),
  hold_out  = 100,
  fh        = 150,
  p         = 0,
  fit_args  = list(
    lower = c(0.1, 6, -0.03, 0.8),
    upper = c(0.9, 13, -1e-14, 1e6),
    m_init = 0.2, o_init = 12, num_searches = 1, mode = "MPL"
  )
)
lag$t1_star
print(chi_sq_np_plot(lag))
print(chi_sq_lambda_plot(lag))
}

}
\references{
Demos, G., & Sornette, D. (2019). Birth or burst of financial bubbles:
which one is easier to diagnose? Quantitative Finance, 17(5), 657-675.
(arXiv:1707.07162)
}
\seealso{
\code{\link[=chi_sq_np_plot]{chi_sq_np_plot()}}, \code{\link[=chi_sq_lambda_plot]{chi_sq_lambda_plot()}}, \code{\link[=fit_lppls]{fit_lppls()}}
}
