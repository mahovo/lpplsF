% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{contour_plot_sse}
\alias{contour_plot_sse}
\title{Create Custom Contour Plot for SSE}
\usage{
contour_plot_sse(
  log_p,
  t,
  par,
  vars,
  lower = c(0, -0.001),
  upper = c(1, 0.001),
  cp = TRUE,
  sp = FALSE,
  fb = FALSE,
  point = c(NA, NA),
  mode = 0
)
}
\arguments{
\item{log_p}{Numeric vector of log-prices.}

\item{t}{Numeric vector of time indices.}

\item{par}{Named list of fixed parameter values.}

\item{vars}{Named list specifying x and y variables as character strings.}

\item{lower}{Numeric vector of length 2, lower bounds for x and y axes.}

\item{upper}{Numeric vector of length 2, upper bounds for x and y axes.}

\item{cp}{Logical, whether to generate contour plot (default TRUE).}

\item{sp}{Logical, whether to generate surface plot (default FALSE).}

\item{fb}{Logical, whether to print feedback (default FALSE).}

\item{point}{Numeric vector of length 2, optional point to highlight.}

\item{mode}{Integer, LPPLS model mode (default 0).}
}
\value{
List with elements: contour_plot, surface_plot (both may be NULL).
}
\description{
Generate a contour plot of the sum of squared errors as a function of
two parameters, with the remaining parameters fixed.
}
\examples{
\dontrun{
# Create contour plot for m vs omega
par <- list(tc = 250, A = 4, B = -0.015, C1 = 0.002, C2 = 0.001)
vars <- list(x = "m", y = "omega")
result <- contour_plot_sse(log_p, t, par, vars,
                           lower = c(0.1, 6), upper = c(0.9, 13))
print(result$contour_plot)
}

}
