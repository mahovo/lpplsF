% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr_trend_test.R
\name{lr_trend_test}
\alias{lr_trend_test}
\title{Autocorrelation-robust significance test for the Lagrange \eqn{\chi^2_{np}} trend}
\usage{
lr_trend_test(rolling, log_price, B = 199, cores = 1L, hac = TRUE, seed = NULL)
}
\arguments{
\item{rolling}{An \code{"lppls_rolling"} object from \code{\link[=lppls_rolling]{lppls_rolling()}}; it must carry the
\code{fit_args} slot (rebuild with a current version of the package if absent).}

\item{log_price}{Numeric vector of log-prices the \code{rolling} was built on; its length
must equal \code{rolling$n_full}.}

\item{B}{Integer, number of bootstrap replicates (default \code{199}).}

\item{cores}{Integer, number of cores for \code{\link[parallel:mclapply]{parallel::mclapply()}} (default \code{1};
ignored on Windows).}

\item{hac}{Logical, whether to also compute the Newey--West HAC \eqn{p}-value
(default \code{TRUE}).}

\item{seed}{Integer or \code{NULL}; a base seed that makes the bootstrap paths
reproducible.}
}
\value{
An object of class \code{"lr_trend_test"}: a list with the observed slope \code{beta1}
and \code{lambda} (\eqn{= -\beta_1}); the naive \code{p_ols} / \code{se_ols}; the HAC \code{p_hac} /
\code{se_hac}; the bootstrap \code{p_boot}, standardized distance \code{z} and null vector \code{null};
the fitted noise model (\code{ar1}, \code{garch} = \code{c(omega, alpha1, beta1)}, and
\code{persist} \eqn{= \alpha + \beta}) with its \code{valid} flag; the effective replicate
count \code{B}, the window count \code{n_windows}, and the \code{call}.
}
\description{
Assesses whether the decreasing "bias" trend that Lagrange regularization acts on
(the slope \eqn{\beta_1} of \eqn{\chi^2_{np}(T_1)} against the window start
\eqn{T_1}) is statistically distinguishable from what the estimation noise produces
on its own. Three tests are reported: a naive OLS \eqn{p}-value (invalid here,
because adjacent rolling windows overlap and \eqn{\chi^2_{np}(T_1)} is heavily
autocorrelated); a heteroskedasticity- and autocorrelation-consistent (Newey--West)
\eqn{p}-value; and a \strong{parametric AR(1)--GARCH(1,1) bootstrap}, which is the
appropriate test.
}
\details{
The bootstrap fits an AR(1)--GARCH(1,1) model (via \code{\link[stats:arima]{stats::arima()}} and the
\pkg{tsgarch} package) to the residuals of the full-window (\eqn{T_1 = 1}) LPPLS fit,
simulates fresh noise paths, adds them to the fitted LPPLS curve, and repeats the
rolling calibration to build the null distribution of \eqn{\beta_1}. The observed
slope is compared against this null (two-sided). A validity guard is applied: if the
fitted GARCH persistence \eqn{\alpha + \beta \ge 1} (IGARCH) the single-LPPLS null is
misspecified, so the bootstrap is skipped, a warning is issued and \code{p_boot} is \code{NA}.

Requires the suggested packages \pkg{tsgarch}, \pkg{xts} and (for \code{hac = TRUE})
\pkg{sandwich}.
}
\examples{
\dontrun{
roll <- lppls_rolling(log_p, t1_seq = seq(1, 1001, 25), hold_out = 100, fh = 150,
                      fit_args = list(mode = "F2", num_searches = 1))
lr_trend_test(roll, log_price = log_p, B = 199, cores = 4, seed = 1)
}

}
\seealso{
\code{\link[=lppls_lagrange]{lppls_lagrange()}}, \code{\link[=lppls_rolling]{lppls_rolling()}}
}
