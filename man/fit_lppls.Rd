% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lppls_fitter.R
\name{fit_lppls}
\alias{fit_lppls}
\title{Fit LPPLS Model to Financial Time Series}
\usage{
fit_lppls(
  log_price,
  fh = 30,
  hold_out = 15,
  lower = c(0.1, 6, -1e+14, 0.8),
  upper = c(0.9, 13, -1e-14, 1e+06),
  tc_init = 1000,
  m_init = 0.5,
  o_init = 13,
  num_searches = 20,
  mode = "F2",
  mpl_plot = FALSE,
  mpl_cutoff = c(0.05, 0.1, 0.5),
  fp = FALSE,
  cp = FALSE,
  sp = FALSE,
  tp = c(0, 0, 0),
  tp_id = 1,
  pp = FALSE,
  mp = FALSE,
  factr = 1e-08,
  fb = FALSE
)
}
\arguments{
\item{log_price}{Numeric vector of log-prices.}

\item{fh}{Integer, forecast horizon length in time units (default 30).
The search for \code{tc} extends from \code{T2+1} to \code{T2+fh} where \code{fh>1}.}

\item{hold_out}{Integer, number of observations to hold out from the end
of the sample for validation (default 15). That is, the number of data
points after \code{T2}.}

\item{lower}{Numeric vector of length 4, lower bounds for
\code{c(m, omega, B, D)}. Default: \code{c(0.1, 6, -1e14, 0.8)}.\cr
Filimonov 2017:\cr
\eqn{B < 0}, \eqn{D \geq 0.8}, \eqn{0.1 \leq m \leq 0.9}, \eqn{6 \leq \omega \leq 13}.\cr
NOTE: For \code{trace_plot} mode 2 and 3 (\code{bm} and \code{bo}), lower for \code{B} should
be set to the desired minimum value of the B-axis in the contour
plotting.\cr
Example:\cr
\preformatted{lower = c(0.1, 6, -0.03, 0.8)}
If the optimization algorithm goes below the lower limits at any point
the lower limit for the B-axis will be extended automatically.}

\item{upper}{Numeric vector of length 4, upper bounds for
\code{c(m, omega, B, D)}. Default: \code{c(0.9, 13, -1e-14, 1e6)}.}

\item{tc_init}{Numeric, initial value of critical time \code{tc} for optim()
(default 1000). \code{tc_init} should be in \eqn{[T_2+1, T_3]}, where
\eqn{T_3 = T_2 + s}, and \eqn{s} is the forecast horizon.}

\item{m_init}{Numeric, initial value of power law exponent \code{m} for optim()
(default 0.5). \code{m_init} should be in [\code{lower[1]}, \code{upper[1]}].
The algorithm will search for initial values of \code{m} and \code{omega} that give
\code{B<0}. If such values are found, the given init values will be
overwritten.}

\item{o_init}{Numeric, initial value of frequency \code{omega} for \code{optim()}
(default 13). \code{o_init} should be in [\code{lower[2]}, \code{upper[2]}]. Default is 13,
which increases the probability of \code{B<0} (of course not for random initial
values). The algorithm will search for initial values of \code{m} and \code{omega}
that give \code{B<0}. If such values are found, the given init values will be
overwritten.}

\item{num_searches}{Integer, number of times to repeat the optimization wrt
\code{tc}, \code{m} and \code{omega}. First iteration uses given initial values. Any
subsequent iterations use random initial values.}

\item{mode}{Character, optimization mode:
\describe{
\item{F1}{Optimize wrt \code{tc}, \code{m}, \code{omega} simultaneously}
\item{F2}{Optimize wrt \code{m}, \code{omega} first (for each \code{tc}), then optimize
wrt \code{tc}}
\item{MPL}{Modified Profile Likelihood with likelihood intervals}
}}

\item{mpl_plot}{Logical, whether to generate MPL plot (default \code{FALSE}).}

\item{mpl_cutoff}{Numeric vector of length 3, cutoff levels (\eqn{c}) for
likelihood interval (default \code{c(0.05, 0.1, 0.5)}). See Filimonov2017
equation (39). \eqn{c = 0.05} indicates that values of tc outside the
likelihood interval have a probability of 0.05.}

\item{fp}{Logical, whether to generate fit plot (default \code{FALSE}).}

\item{cp}{Logical, whether to generate contour plot (default \code{FALSE}). \code{tc}
value is fixed to the estimate with the lowest objective function value.}

\item{sp}{Logical, whether to generate surface plot (default \code{FALSE}). \code{tc}
value is fixed to the estimate with the lowest objective function value.}

\item{tp}{Numeric vector of length 3, 0/1 selectors for trace plots.
Plot 1: \code{m-omega}, plot 2: \code{B-m}, plot 3:\code{B-omega} (default \code{c(0, 0, 0)}).\cr
Example: \code{c(1, 0, 1)} selects trace plot 1 and 3. Traces convergence of
parameters during optimization of F2 objective function. Only works when
\code{mode = "F2"}. The plotted trace path is the one for the value of \code{tc} that
produced the smallest value of \code{SSE2}.\cr
NOTE: Contour plots in trace plot 2 and 3 only apply to the first step in
the trace. In a trace plot wrt \code{B} and \code{m}, \code{omega} is not fixed (and vice
versa for \code{m} and \code{omega}).}

\item{tp_id}{Integer, which trace step to use for contour plot (default 1).
If the indicated id is too high, 1 will be chosen.}

\item{pp}{Logical, whether to generate parameter plot (default \code{FALSE}).}

\item{mp}{Logical, whether to generate matrix plot (default \code{FALSE}).
x-axis is \code{tc}, y-axis is \code{SSE}.
3 columns of plots for smallest and biggest value of \code{m}, and value between
the two.
3 columns of plots for smallest and biggest value of \code{omega}, and value
between the two.}

\item{factr}{Numeric, convergence tolerance for optim control list when
using L-BFGS-B. See \code{optim()} documentation.}

\item{fb}{Logical, whether to print feedback during execution (default
\code{FALSE}).}
}
\value{
A list containing:
\describe{
\item{fit}{List with fitted model results. In F1/F2 mode:\cr
\describe{
\item{mode = "F1"}{List of fits with random initial values.
\describe{
\item{\code{fit[[1]]}}{
list of best fit coefficients optimized wrt \code{m} and \code{omega}.
}
\item{\code{fit[[2]]}}{
Tibble of fits with random starting parameters.\cr
Sorted by objective function value (best at top).\cr
Output format:\cr
\preformatted{
                [
                  list(tc, m, omega, A, B, C1, C2, D, value_min, ID),
                  list(tc, m, omega, A, B, C1, C2, D, value, ID),
                  ...,
                  list(tc, m, omega, A, B, C1, C2, D, value_max, ID)
                ]
              }
}
\item{\code{fit[[3]]}}{
If no fits passed the filter: Only unfiltered fits returned.
}
}
\code{ID} is an identifier indicating the order pre-sorting. These are the
indexes reported in "out of range" warnings.\cr
}
\item{mode = "F2"}{List with two or three elements:
\describe{
\item{\code{fit[[1]]}}{
List of best fit coefficients optimized wrt \code{m} and \code{omega}.
}
\item{\code{fit[[2]]}}{
If at least one fit passed the filter: Tibble of best fits for
objective function \code{F2} for each value of \code{tc}. Columns are:
\code{c(ID, value, tc, m, omega, A, B, C1, C2, damp)}. Sorted by
objective function value (best at top). For each value of \code{tc}
the best fit is picked from \code{fit2_tmp}.\cr
If no fits passed the filter: Only unfiltered fits returned.
}
\item{\code{fit[[3]]}}{
If at least one fit passed the filter: Unfiltered fits returned.\cr
If no fits passed the filter: Nothing is returned in \code{fit[[3]]}
}
\item{\code{fit[[4]]}}{
List of all tmp fits\cr
For each \code{tc} value, there is a tibble with all fits using random
starting points.
}
}
}
}
}
\item{mpl_output}{(MPL mode only) List with likelihood intervals,
R values, log-likelihoods\cr
\code{mpl = list(LI, R, LL, MLL)}\cr
\describe{
\item{LI}{vector of 3 elements, one for each value of \code{mpl_cutoff}.}
\item{R}{number, relative likelihood}
\item{LL}{vector of MPL log-likelihoods for each \code{tc} value}
\item{MLL}{maximum of \code{LL}}
\item{tc_hat_mpl}{\code{tc} for which \code{LL} takes it's maximum}
}
}
\item{mpl_plot}{(if requested) ggplot2 object for Modified Profile
Likelihood plot with likelihood intervals}
\item{fit_plot}{(if requested) ggplot2 object for fit plot}
\item{contour_data}{(if requested) List with \code{x}, \code{y} and \code{z} data for contour
plot of SSE wrt \code{m} and \code{omega}.}
\item{contour_plot}{(if requested) plotly object for contour plot}
\item{surface_plot}{(if requested) plotly object for surface plot}
\item{trace_plot_mo}{(if requested) ggplot2 object containing trace plot
for \code{m} and \code{omega}}
\item{trace_plot_bm}{(if requested) ggplot2 object containing trace plot
for \code{B} and \code{m}}
\item{trace_plot_bo}{(if requested) ggplot2 object for containing trace
plot for \code{B} and \code{omega}}
\item{param_plot}{(if requested) ggplot2 object for parameter plot}
\item{matrix_plot}{(if requested) ggplot2 object for matrix plot}
\item{out_of_range_tracker}{List tracking which fits had out-of-range
\code{B} or damping values\cr
\describe{
\item{F1 mode}{List of random iteration IDs what resulted in
out-of-range parameters}
\item{F2 mode}{List of random tc value IDs and iteration IDs what
resulted in out-of-range parameters.\cr
\code{tc} value ID start with 1 for \code{T2+1}. That is, the first time step
after the modelling period, or the first time step of the
forecasting horizon.}
}
}
}
}
\description{
Fits the Log-Periodic Power Law Singularity (LPPLS) model to log-price
data using the calibration methods from Filimonov & Sornette (2013).
Supports multiple optimization modes and provides diagnostic outputs.
}
\details{
The LPPLS model is:
\deqn{y(t) = A + (t_c - t)^m [B + C_1 \cos(\omega \log(t_c - t)) + C_2 \sin(\omega \log(t_c - t))]}

The model has 7 parameters: \eqn{A}, \eqn{B}, \eqn{C_1}, \eqn{C_2} (linear) and \eqn{t_c}, \eqn{m}, \eqn{\omega} (nonlinear).
The Filimonov calibration exploits this structure by solving for the linear
parameters analytically given the nonlinear ones.

\strong{Mode F1}: Optimizes all nonlinear parameters \code{c(tc, m, omega)} simultaneously
using L-BFGS-B with multiple random starting points.

\strong{Mode F2}: A two-stage procedure:
\enumerate{
\item For each candidate \code{tc} value, optimize \code{c(m, omega)}
\item Select the \code{tc} that minimizes the overall SSE
}
This is more robust than F1 for ill-conditioned problems.

\strong{Mode MPL}: Modified Profile Likelihood inference from Filimonov et al. (2017).
Provides likelihood-based confidence intervals for \code{tc}.
}
\examples{
\dontrun{
# Generate synthetic bubble data
set.seed(123)
t <- 1:200
true_params <- list(A = 4, B = -0.015, C1 = 0.002, C2 = 0.001,
                    tc = 250, m = 0.5, omega = 9)
log_p <- eval_lppls(t, true_params$A, true_params$B, true_params$C1,
                   true_params$C2, true_params$tc, true_params$m,
                   true_params$omega) + rnorm(200, 0, 0.01)

# Fit model
result <- fit_lppls(log_price = log_p,
                 fh = 100, hold_out = 0,
                 tc_init = 250, mode = "F2",
                 num_searches = 10, fp = TRUE)

# View best fit parameters
print(result$fit[[1]])

# View fit plot
print(result$fit_plot)
}

}
\references{
Filimonov, V., & Sornette, D. (2013). A stable and robust calibration scheme
of the log-periodic power law model. Physica A, 392(17), 3698-3707.

Filimonov, V., Demos, G., & Sornette, D. (2017). Modified profile likelihood
inference and interval forecast of the burst of financial bubbles.
Quantitative Finance, 17(8), 1167-1186.
}
